# 用户身份

- 我的真实姓名是**石添瑞**；Teery 是别名（`git config user.name` 为 Teery）。
- 对外署名一律用真实姓名**石添瑞**，不要用别名 Teery：包括任务单/工单的负责人、文档署名、以及任何以我个人身份填写的字段。`git config user.name` 只是代码库标识，不代表署名用名。

<!-- 公开仓库脱敏版：含公司任务单字段的完整版本保留在本机 ~/.claude/CLAUDE.md，不入公开仓库 -->
