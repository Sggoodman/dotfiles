# 自定义 Skill 备份

每个子目录是一个 skill，恢复时整体复制到 `~/.claude/skills/`。

| Skill | 用途 |
|---|---|
| *（暂无可公开的 skill）* | 含公司信息的 skill（如绩效相关）存放在私有仓库 `private-dotfiles` |

> 当前 `~/.claude/skills/` 下如有没有列出的 skill，说明它含敏感内容，在私有仓库 `claude-skills/` 目录里。
